export type EMAIL = string;
export type FIRSTNAME = string;
export type LASTNAME = string;
export type PASSWORD = string;

export enum ROUTES {
  AUTHENTICATION_ROOT_PATH = '/auth',
  USER_ROOT_PATH = '/user',
}